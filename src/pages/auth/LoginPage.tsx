import { NavBarWrapper } from '@/components/layout/wrappers';

export default function LoginPage() {
    return (
        <NavBarWrapper>
            <div>Login</div>
        </NavBarWrapper>
    );
}
